// import { fromJS } from 'immutable';
// import { makeSelectFirebaseChatDomain } from '../selectors';

// const selector = makeSelectFirebaseChatDomain();

describe('makeSelectFirebaseChatDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
